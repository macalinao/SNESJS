---
source_directory: source
build_directory: build

layout: together

header: HEADER.txt

builds:
  src:
    packer: false
  min:
    shrink_vars: true
    private: true

packages:
  snesjs:
    files:
    - SNESJS
    - Constants
    # CPU
    - cpu/CPU
    - cpu/ALU
    - cpu/CPUOPS
    - cpu/MMIO
    - cpu/Registers
    - cpu/Status
    # Util
    - util/Bits
    - util/MiscUtils
